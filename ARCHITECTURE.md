# 技術ドキュメント

平面曲線の微分幾何学(接線・法線・曲率・接触円・縮閉線・曲率櫛)をインタラクティブに探索するWebページの実装解説です。利用者向けの説明は [README.md](./README.md) を参照してください。ここではコードの構成・数学的背景・設計判断をまとめます。

## 全体構成

ビルドステップを持たない静的サイトです。バンドラ・フレームワーク・外部ライブラリは一切使用していません(three.jsやmath.jsなども不使用)。ブラウザ標準のCanvas 2D APIとES Modulesのみで完結しています。

```
index.html          エントリーポイント。UIのDOM構造を定義
style.css            モノクロ(黒背景)のスタイル。CSSカスタムプロパティでグレースケールを一元管理
js/curves.js         曲線プリセットの定義(x(t), y(t) とパラメータ範囲)
js/expr.js            カスタム曲線用の数式パーサー(eval不使用)
js/geometry.js        数値微分・曲率・弧長などの微分幾何計算
js/render.js          Canvas描画(曲線・ベクトル・接触円・曲率グラフなど)
js/main.js            状態管理・イベント配線・アニメーションループ
wrangler.toml          Cloudflare Workers(静的アセット配信)用の設定
.assetsignore          デプロイ時にアップロードしないファイルの指定
```

依存関係は `devDependencies` の `wrangler`(デプロイ用CLI)のみで、実行時に読み込むライブラリはありません。

## データフロー

```
curves.js の曲線定義 (x(t), y(t), t0, t1)
        │
        ▼
geometry.js: sampleCurve() が t0〜t1 を N+1 点(既定900)でサンプリングし、
             各点で frameAt() を呼んで位置・接線・法線・曲率等をまとめた
             「フレーム」の配列を作る
        │
        ▼
main.js の state.samples に保持。曲線選択やカスタム式の適用時に再計算
        │
        ▼
render.js の各 draw* 関数が state.samples とスライダー位置 state.t を
          受け取り、Canvas に毎フレーム描画する(render() 関数)
```

`main.js` はスライダー入力・ドラッグ・自動再生のいずれの場合も、最終的に `state.t` を更新して `render()` を呼ぶだけのシンプルなループになっています。

## 数学的背景 (`js/geometry.js`)

平面曲線 `(x(t), y(t))` に対して、以下の微分幾何の標準公式を使っています。

- 速度ベクトル: `v(t) = (x'(t), y'(t))`
- 単位接線ベクトル: `T = v / |v|`
- 単位法線ベクトル: `N = (-T_y, T_x)`(進行方向左向きに90°回転)
- 符号付き曲率: `κ = (x'y'' − y'x'') / (x'² + y'²)^(3/2)`
- 曲率半径: `R = 1/κ`
- 接触円(曲率円)の中心: `C = P + R・N`
- 弧長: `s(t) = ∫ |v(τ)| dτ`(t0からtまで)
- 縮閉線(evolute): 各 t における接触円中心 `C(t)` の軌跡

### 数値微分

`x'(t)`, `y'(t)`, `x''(t)`, `y''(t)` はすべて中心差分で計算しています(`velocity()`, `acceleration()`)。

```
f'(t)  ≈ (f(t+h) − f(t−h)) / (2h)
f''(t) ≈ (f(t+h) − 2f(t) + f(t−h)) / h²
```

ステップ幅 `h` は `makeDiffStep()` でパラメータ区間の長さに比例させています(`span * 1e-5`、下限 `1e-6`)。解析的な導関数を曲線ごとに手書きする代わりにこの方式を採用したことで、プリセット曲線もカスタム曲線(`expr.js` で数式から作った関数)も **同じ `frameAt()` 一本で** 扱えます。

### カスプ(尖点)での特異点処理

アステロイドやサイクロイドのようにカスプを持つ曲線では、カスプ上で速度 `|v| → 0` になります。曲率の公式はこの `|v|` の3乗で割るため、数値誤差同士の商が爆発し、`κ` が非現実的に巨大な有限値(例: `1.37×10^15`)になってしまう問題がありました。

`frameAt(curve, t, h, minSpeed = 1e-3)` では、生の速度が `minSpeed` を下回った場合を「特異点」とみなし、

1. 曲率は `NaN`(未定義)として扱う(描画側は `Number.isFinite` でフィルタし、数値パネルには `∞` と表示)
2. 接線方向はステップ幅を200倍に広げて再計算し、カスプの前後で安定した向きを推定する

という処理をしています。これにより、曲率グラフや曲率櫛のスケールが1点の異常値に引きずられて他の部分が見えなくなる、という問題も同時に解消しています。

## カスタム曲線の数式パーサー (`js/expr.js`)

利用者が入力する `x(t)`, `y(t)` は `eval()` や `new Function()` を使わず、専用のトークナイザ+再帰下降パーサーで構文解析し、AST(構文木)を経由して評価しています。

- 演算子: `+ - * / ^`(べき乗は右結合)、単項マイナス
- 関数: `sin cos tan asin acos atan atan2 exp log log2 log10 sqrt abs pow min max sign`
- 定数: `pi`, `e`
- 変数: `t`

文法(EBNF風):

```
expr   := term (('+' | '-') term)*
term   := unary (('*' | '/') unary)*
unary  := ('+' | '-') unary | power
power  := primary ('^' unary)?
primary:= number | identifier ('(' args ')')? | '(' expr ')'
args   := expr (',' expr)*
```

`compileExpression(src)` はパース後に一度 `t = 0.37` で評価してみて、構文エラーだけでなく実行時エラー(未定義関数など)もこの時点で検出します。エラーは日本語メッセージにして `main.js` 側でUIに表示します(`#custom-error`)。

外部の数式ライブラリ(math.js等)を使わず自前実装にしたのは、実行時に任意コードを評価する `eval`/`Function` を避けるためです。

## 描画 (`js/render.js`)

- `fitCanvas()`: `devicePixelRatio` に合わせてCanvasの実ピクセルサイズを設定し、`ctx.setTransform` でCSSピクセル単位の座標系に統一(Retina対応)。
- `makeView(box, width, height)`: 曲線のバウンディングボックスをCanvasに余白付きでフィットさせる、数学座標→Canvas座標の変換(`toCanvas`)を返す。y軸は反転(数学ではy上向き、Canvasはy下向き)。
- 描画レイヤーは背面から順に: グリッド → 縮閉線 → 曲線(未到達部分は薄いグレー、到達済み部分は太い白)→ 曲率櫛 → 接触円 → 法線・接線ベクトル → 現在点。
- 色は `INK` オブジェクトに一元化した完全なモノクロパレット(黒背景に対して白〜グレーの階調のみ、色相は使わない)。強調したい要素ほど白に近く、補助的な要素ほど暗いグレーにしてある。
- 曲率グラフ(`drawCurvatureGraph`)は `κ(t)` を折れ線で描画。外れ値(カスプ付近など)でスケールが壊れないよう、`|κ|` の95パーセンタイルを基準に縦軸をクリップしている。

## 状態管理とインタラクション (`js/main.js`)

- `state` オブジェクトが単一の真実源(現在の曲線・サンプル配列・バウンディングボックス・現在の `t`・再生中フラグなど)。
- 曲線切り替え・カスタム式の適用時に `setCurve()` が `sampleCurve()` を呼び直し、スライダーの `min/max/step` を曲線のパラメータ範囲に合わせて更新。
- スライダー操作・自動再生(`requestAnimationFrame` ベース)・Canvas上のポインタドラッグ(サンプル点の中から最近傍を探索して `t` を決定)は、いずれも最終的に `state.t` を更新して `render()` を呼ぶだけの単純なフローに統一されている。
- 自動再生は周期的な曲線(円・楕円など)ではループし、非周期的な曲線(放物線など)では終端で停止する。

## Cloudflareへのデプロイ構成

`wrangler.toml` は `[assets] directory = "."` によってリポジトリのルートを静的アセットとして配信する構成です(Workerスクリプトなし)。`.assetsignore` で `.git`・`README.md`・`package.json` など配信不要なファイルを除外しています。デプロイ経路の詳細・トラブルシューティングは [README.md](./README.md) を参照してください。

## 既知の制限

- 数値微分ベースのため、極端に振動の速い(高周波の)カスタム曲線ではステップ幅 `h` に対して精度が落ちる場合がある。
- カスプ判定の `minSpeed`(既定 `1e-3`)は座標のスケールに対する絶対値のしきい値であり、極端に小さい/大きいスケールのカスタム曲線では調整が必要になる場合がある。
- 曲率櫛のスケールはサンプル中の `|κ|` の最大値(有限値のみ)を基準に自動計算されるため、曲率が局所的に大きく変化する曲線ではティックの長さの見た目が変わることがある。
